from setuptools import setup, find_packages

setup(
    name = "representability",
    version = "0.0.0",
    author = "Nicholas Rubin",
    author_email = "nick@rigetti.com",
    description = "A library for constructing dual operators!",
    url="git",
    packages = find_packages(),
)
